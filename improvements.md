# Improvements
- Automatically add all words as custom if they didnt exist before, when finishing the word (spacebar). Also set limit of maximum new custom words to 20000
- If no words matching, automatically go into abc mode (where you manually pick characters by pressing the key multiple times). When the word is finished (spacebar), go back to predictive mode
- When clicking on a word, go to the end of the word and start editing as normal (predictive mode)